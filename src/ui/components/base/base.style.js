import { Width, Height } from 'utils/Layout'
import { StyleSheet } from 'react-native'
import { theme } from 'theme'

export const Styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        backgroundColor: theme.themeApp.backgroundBody
    },


})